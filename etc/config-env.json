{
    "timeZone": "{{TIME_ZONE}}",
    "domainName": "{{DOMAIN_NAME}}",
    "httpServerPort": "{{HTTP_SERVER_PORT}}",

    "telegramBotToken": "{{TELEGRAM_BOT_TOKEN}}",
    "telegramBotUpdates": "{{TELEGRAM_BOT_UPDATES}}",

    "database": {
        "protocol": "{{DB_PROTOCOL}}",
        "host": "{{DB_HOST}}",
        "port": "{{DB_PORT}}",
        "name": "{{DB_NAME}}",
        "user": "{{DB_USER}}",
        "password": "{{DB_PASSWORD}}"
    }
}